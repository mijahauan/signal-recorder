# GRAPE Core Recorder Configuration
# Minimal recorder: RTP → NPZ archives only (no analytics)

# RTP multicast configuration
multicast_address = "239.103.26.231"  # ka9q-radio IQ stream
port = 5004

# Output directory for NPZ archives
output_dir = "/tmp/grape-core-test"

# Station metadata
[station]
callsign = "AC0G"
grid_square = "EM38ww"
instrument_id = "172"

# Channels to record
[[channels]]
ssrc = 2500000
frequency_hz = 2500000
sample_rate = 16000
description = "WWV 2.5 MHz"

[[channels]]
ssrc = 5000000
frequency_hz = 5000000
sample_rate = 16000
description = "WWV 5 MHz"

[[channels]]
ssrc = 10000000
frequency_hz = 10000000
sample_rate = 16000
description = "WWV 10 MHz"
